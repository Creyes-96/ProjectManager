<%= form_with(model: project_phase_milestone_note) do |form| %>
  <% if project_phase_milestone_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_phase_milestone_note.errors.count, "error") %> prohibited this project_phase_milestone_note from being saved:</h2>

      <ul>
        <% project_phase_milestone_note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_field :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
