<%= form_with(model: project_phase_activity) do |form| %>
  <% if project_phase_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_phase_activity.errors.count, "error") %> prohibited this project_phase_activity from being saved:</h2>

      <ul>
        <% project_phase_activity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
