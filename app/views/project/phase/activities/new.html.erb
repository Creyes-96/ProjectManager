<% if can? :manage, @project_phase_activity %>
    <h1>New Project Phase Activity</h1>

    <%= render 'form', project_phase_activity: @project_phase_activity %>

<% end %>

<%= link_to 'Back', project_phase_activities_path %>
