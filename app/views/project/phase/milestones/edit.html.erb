<% if can? :manage, @project_phase_milestone %>
    <div class="view-title">
        <h1>Editing <%= @phaseedit.name%> milestone</h1>
    </div>

    <%= render 'form', project_phase_milestone: @project_phase_milestone %>

    <%= link_to 'Show', @project_phase_milestone %> |
<%end%>
<%= link_to 'Back', project_phase_milestones_path %>
